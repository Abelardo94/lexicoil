# Estado — sesión del 17 sep 2026

Rama: `feat/en-pool-first`. **Nada commiteado**: todo lo de abajo está en el árbol de
trabajo, sin `git add`. Continúa `docs/handoff/ESTADO-FASE1-2026-09-03.md`.

## Lo que se cierra

**Cola de decisiones de la Fase 1: el punto 1 (ambigüedades de P5) y el punto 2
(ortografía) están hechos. El punto 5 (gap-fill sin verificar) queda medido y con
un bloqueo identificado.**

## 1. La muestra ciega, terminada sin gastar tu tiempo

Llevabas 8 de 36 (paradas el 4 sep, 00:31, todas `ok`). Las 28 restantes las revisó
**Fable** a ciegas — sin ver el veredicto de SEM-1 ni la clave — y se cruzaron con
`docs/audit/blind-sample-en_B1.key.json`.

Control positivo que nadie le dio: **redescubrió por su cuenta los dos defectos que tú
ya habías verificado a mano** (`range`/`variety` y `pack`/`carry`).

```
Reparto de SEM-1 en esos 28:  passed 15 · flagged 4 · unchecked 9

Sobre los 19 que SEM-1 sí evaluó:
  falsos positivos   0       (los 4 que marcó son defectos reales)
  falsos negativos   1       n24, y de confianza baja
  acuerdos          18

Sobre los 9 que SEM-1 no mira (gap-fill):
  defectos           2       n26, n28
```

**La conclusión que bloqueaba la Fase 1: SEM-1 sirve para las 427 partes de la Etapa 2,
pero solo para los tipos que inspecciona.** Donde mira, su precisión es 4/4 y se le
escapó uno dudoso de 19. El agujero no es su criterio, es su cobertura: en el 22 % de
lo que no mira hay defectos.

Los 7 defectos son **todos la misma cosa**: ambigüedad léxica en cloze de una palabra
(`lesen` T5 y T6). Ni uno solo de otro tipo.

## 2. Ortografía — 45 correcciones, y estaban en producción

No era solo la piscina: las mismas cadenas viven en `library/curated/` y en
`data/exams/en_B1.json`, que es **lo que se sirve hoy**.

```
node scripts/fix-en-spelling-variant.mjs --apply
→ 151 correcciones en 5 ficheros (45 formas distintas × las 3 copias del contenido)
   center→centre · realized→realised · armor→armour · jewelry→jewellery
   favorite→favourite · colorful→colourful · traveling→travelling · organizers→organisers
```

**Coste en TTS: 5 transcripciones distintas**, no las 24 que sugería el recuento por
fichero (el mismo audio está repetido en semilla + curated + servido). Al lado de los
732 clips que ya se deben, es ruido.

`learned×12` y `program×3` se reportan y **no se tocan**: son correctas en británico
según el sentido.

## 3. Reading P5 — eran 4 ambigüedades, no 2, y había un defecto duro

El doc del 3 sep recogía 2. Las partes en cuarentena son **dos**, con dos hallazgos cada
una (`sem1Issues`), y la segunda no estaba documentada.

Y por debajo había uno que **ni SEM-1 ni la muestra ciega habían visto**:

```
ql_en-b1-r-t5-cloze-cooking-class-01-q1
  ["a) knew", "b) know", "c) knew", "d) known"]   clave: "c"
```

`a)` y `c)` son la misma palabra. Quien marcaba `a) knew` escribía la respuesta correcta
y se le puntuaba como fallo. Estaba servido en producción.

Barrido completo: **1 ítem en 462 preguntas** en inglés, **0 en 6.485** del alemán
(`de/B1` 5.850 + `de/A2` 635). Es aislado, no sistémico.

### Las 6 reparaciones

Todas cambian **un distractor**, nunca la clave, así que la letra correcta que guardan
los resultados ya almacenados no se mueve. Tampoco se toca el texto del pasaje.

| ítem | cambio | por qué |
|---|---|---|
| cooking q1 | `a) knew` → `a) knowing` | era la misma opción que la clave `c) knew` |
| cooking q3 | `b) told` → `b) spoke` | "told us what to do" vale igual que "showed" |
| cooking q5 | `c) cooked` → `c) bought` | "cooked a meal" vale igual que "prepared" |
| cooking q5 | `d) finished` → `d) ordered` | **no estaba en el informe**: "finished a meal" también se sostiene |
| festival q4 | `a) carry` → `a) collect` | "carry a jacket" vale igual que "pack" |
| festival q5 | `d) variety` → `d) amount` | "a wide variety of food" es tan idiomático como "a wide range of" |

Verificado volviendo a pasar SEM-1 por las dos partes: **pasan las dos, 0 issues**,
$0,0010. La piscina queda **36/36 sin cuarentena**.

## 4. Dos gates nuevos, los dos probados saboteados

`scripts/audit-en-spelling-variant.mjs` (reescrito) y
`scripts/audit-en-duplicate-options.mjs` (nuevo), con sus tests.

**Van en el CI antes de `ci:content`, a propósito.** `ci:content` muere en la fidelidad
del alemán, y encadenado con `&&` un gate nuevo no llegaría a ejecutarse nunca: verde
por no correr, que es justo lo que estos gates existen para evitar.

Dos cosas que salieron de probarlos saboteados y que conviene no olvidar:

- **El gate de ortografía daba verde sin mirar.** Su recorrido entraba en los arrays
  pero descartaba los elementos que no fueran objetos, así que no leía **ni una sola
  opción de respuesta** — donde estaban 40 de las 45 apariciones. El fallo apareció por
  tener dos copias del mismo recorrido (la del corrector estaba bien). Ahora hay una
  sola, `mapStrings()` en `scripts/lib/britishSpelling.mjs`, y un test que la sabotea.
- El gate anterior marcaba **`laboratory`** (por `\blabor`) y **`practice`** (correcta
  como sustantivo en británico), y contaba `organizers` dos veces. El emparejamiento
  ahora es por palabra completa contra una tabla con las flexiones.

## 5. Revisado por Danilo el mismo día — los 3 pendientes, cerrados

Aprobó las 6 sustituciones, el arreglo de n24 y la conversión de los huecos léxicos.

**n24** — `c) explore` → `c) listen`. "explore new music" es una colocación real y se
sostenía frente a la clave `discover`; "listen new music" falla por gramática (pide *to*),
el mismo criterio que ya usan `said` y `explained` en cooking q3.

**Los huecos léxicos del Part 6 — eran 5, no 3.** `next` y `spending` se habían
clasificado a ojo como "forzados en la práctica" y **no lo estaban**: "return **every**
year" y "I enjoyed **my** time with my colleagues" valen igual de bien. Lo siguió diciendo
el gate hasta que se releyeron. Los cinco:

| clave vieja | hueco nuevo | clave | por qué es única |
|---|---|---|---|
| `made` | `we made (2) ___ compromise` | `a` | "make a compromise" exige artículo indefinido |
| `lots` | `took lots of photos (4) ___ show` | `to` | infinitivo de finalidad |
| `never` | `an advertisement (1) ___ the local newspaper` | `in` | "on the newspaper" no existe |
| `next` | `We all agreed (6) ___ we would…` | `that` | "agree that" no admite otra |
| `spending` | `enjoyed spending time (3) ___ my colleagues` | `with` | "spend time with" |

Cada hueco se queda **dentro de su propia frase y conserva su número**, así que el orden
de huecos y la posición de cada respuesta guardada no se mueven. Verificado: los 3
pasajes conservan los 6 huecos en orden ascendente. Son partes de `lesen`, así que no
tocan el TTS.

```
node scripts/audit-en-open-cloze-gaps.mjs
→ 54 huecos · 0 léxicos   (antes: 5 léxicos de 18 en la semilla)
```

Quedan 4 marcados como **gramaticales pero flojos** (`a`, `the`, `that`×2). No bloquean:
son un aviso para releer el hueco, no un defecto.

**Gate nuevo:** `scripts/audit-en-open-cloze-gaps.mjs` + `scripts/lib/openClozeGaps.mjs`,
con lista cerrada de palabras funcionales — a propósito, para que nunca apruebe algo
"probablemente gramatical". 8 pruebas, y saboteado contra el repo real: exit 1.

## Lo que sigue necesitando tus ojos — 3 ítems, no 28

*(Cerrados el 17 sep, ver arriba. Se deja el detalle por si hay que revisar el criterio.)*

1. **n24** — cloze festival, hueco 5, clave `discover`; `explore` es defendible. Es el
   único falso negativo de SEM-1 y Fable lo dio con confianza **baja**. Duda genuina:
   decide tú si es defecto.
2. **n26** (`made`/`reached` a compromise) y **n28** (`lots`/`plenty`/`loads` of photos),
   los dos en Reading T6, hueco abierto.
3. Un repaso de un minuto a las 6 sustituciones de la tabla de arriba.

**n26 y n28 no se arreglan como las otras**: al no haber opciones, no hay distractor que
cambiar. Comprobado que **el motor no soporta respuestas alternativas** — no existe
`acceptedAnswers` ni equivalente en `js/engine` ni en las funciones. Así que solo hay dos
salidas, y es decisión tuya:

- **(a)** reescribir la frase para que solo encaje una palabra — barato, pero cambia el
  texto de lectura de intentos ya guardados;
- **(b)** dar soporte a respuestas válidas múltiples en el motor — es lo que además
  destapa el punto 5 de la cola (los 9 gap-fill que SEM-1 no puede verificar), porque
  hoy ni siquiera hay dónde anotar que dos respuestas valen.

## 6. Un séptimo verde silencioso, encontrado al commitear

`run-sem1-over-seed.mjs` **reescribía** `docs/audit/sem1-en_B1.json` con lo que acababa
de verificar en vez de fundirlo. Al reverificar las 2 partes reparadas, el informe pasó
de **30 resultados a 2**: se perdió la medición del 3 sep que cita
`ESTADO-FASE1-2026-09-03.md`.

Lo grave no es la pérdida — está en git — sino que **rompe en silencio
`build-blind-sample.mjs`**, que lee ese fichero para saber qué ítems marcó SEM-1. Con
solo la última pasada dentro, todos los demás ítems parecen *nunca marcados* y la
muestra ciega se construye mal sin avisar de nada.

Arreglado: el informe se funde por id, los totales se recalculan sobre el conjunto y
`thisRun` lleva lo de la pasada en curso, que era lo que se confundía con el total. El
fichero queda reconstruido con las 28 del 3 sep más las 2 reverificadas: 30 resultados,
30 pasan, 0 hallazgos.

```
node scripts/run-sem1-over-seed.mjs --lang en --level B1 --sample 1
→ Informe → docs/audit/sem1-en_B1.json  (1 de esta pasada, 30 en total)
```

## Verificación

```
npm run test:spelling-gate            9/9
npm run audit:spelling:en             exit 0   (saboteado: exit 1)
npm run test:duplicate-options-gate   7/7
npm run audit:duplicate-options       exit 0   (saboteado: exit 1)
npm run test:open-cloze-gate          8/8
npm run audit:open-cloze:en           exit 0   (saboteado: exit 1)
npm run validate:fidelity:en-b1       3 exámenes, 3 pasan, 0 errores
npm run test:cambridge-scoring        OK
npm run test:listening-gap-fill       OK
npm run test:speaking-hint            OK

node scripts/audit-pass-2.mjs batches/generated
→ 17 / 413 / 237 · 150 archivos · 827 preguntas   = baseline del 3 sep, sin regresión
```

`npm run test:served-exams` sigue fallando (`official-de-B1-e6/e18/e19` drifted).
**Comprobado con `git stash` que falla igual sin estos cambios**: es el rojo previo del
alemán, de la renormalización de `8272af9`.

## Ficheros

Modificados: `data/exams/en_B1.json`, `library/reusable-seed/en_B1.json`,
`library/curated/en/B1/curated_en_B1_{265a91d95c9d,2db2385bdc63,69b490d83b08}.json`,
`docs/audit/sem1-en_B1.json`, `package.json`,
`.github/workflows/content-validation.yml`, `scripts/audit-en-spelling-variant.mjs`.

Nuevos: `scripts/lib/{britishSpelling,openClozeGaps}.mjs`,
`scripts/fix-en-spelling-variant.mjs`, `scripts/audit-en-duplicate-options.mjs`,
`scripts/audit-en-open-cloze-gaps.mjs`, `scripts/repair-en-b1-p5-ambiguities.mjs`,
`scripts/repair-en-b1-t6-lexical-gaps.mjs`,
`scripts/lib/__tests__/{spellingGate,duplicateOptionsGate,openClozeGate}.test.mjs`.

Los dos `repair-*.mjs` son **idempotentes**: se pueden volver a correr y dejan constancia
de cada cambio hecho, con el porqué al lado.

`js/data/savedVocab{Phrases,Quizzes}.js` siguen con el WIP de Marcos sin commitear, sin
tocar. Nada de esto ha subido a producción: sigue desplegando del `main` de Marcos.
